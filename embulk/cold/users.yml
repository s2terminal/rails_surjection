in:
  type: mysql
  host: localhost
  user: root
  password: ""
  database: development_alpha
  table: users
filters:
  - type: column
    columns:
      - { name: id }
      - { name: name }
      - { name: created_at }
      - { name: updated_at }

out:
  type: mysql
  host: localhost
  user: root
  password: ""
  database: development_beta
  table: users
  mode: truncate_insert
